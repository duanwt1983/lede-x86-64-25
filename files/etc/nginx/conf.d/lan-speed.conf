# Same-origin proxy so HTTPS LuCI can iframe LibreSpeed.
location /lan-speed/ {
	proxy_pass http://127.0.0.1:8989/;
	proxy_http_version 1.1;
	proxy_set_header Host $host;
	proxy_set_header X-Real-IP $remote_addr;
	proxy_read_timeout 120s;
}
