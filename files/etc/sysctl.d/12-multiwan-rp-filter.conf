# Dual-WAN DNS/data replies on the non-default WAN are dropped when
# rp_filter=1 (strict): fib says "that dest goes out wan1" so wan2
# ingress is treated as spoofing. Loose mode accepts if any table can
# reach the source.
net.ipv4.conf.all.rp_filter=2
net.ipv4.conf.default.rp_filter=2
